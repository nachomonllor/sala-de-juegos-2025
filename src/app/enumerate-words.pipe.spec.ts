import { EnumerateWordsPipe } from './enumerate-words.pipe';

describe('EnumerateWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumerateWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
